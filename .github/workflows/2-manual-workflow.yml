name: Manual Workflow

on: 
    workflow_dispatch:

jobs:
    first-workflow:
        runs-on: ubumntu-latest

        steps: 
            - name: First Step
              run: | 
                echo "Hello, World!"
                echo "Hello, Again!"